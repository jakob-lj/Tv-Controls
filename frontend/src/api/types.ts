export type TvDevice = {
  name: string;
  id: string;
};

export type DeviceCommand = {
  command: string;
  image: string;
  description: string;
  argument: string;
};

export type JakeboxId = {
  id: string;
};
